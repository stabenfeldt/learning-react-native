import React from 'react-native';
const { AppRegistry } = React;

import WeatherProject from './src/components/weather-project';

AppRegistry.registerComponent('WeatherProject', () => WeatherProject);
